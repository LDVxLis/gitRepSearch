import { describe, it } from "vitest";
describe("RepoItem", () => {
  it("отображает название, звезды, дату", () => {
    // const mockProps = {};
    // render(<RepoItem {...mockProps} />);
    // Проверить, что отображается имя репозитория
    // expect(screen.getByText("")).toBeInTheDocument();
    // Проверить количество звёзд
    // expect(screen.getByText("")).toBeInTheDocument();
    // Проверить дату обновления
    // expect(screen.getByText("")).toBeInTheDocument();
    // Проверить имя владельца
    // expect(screen.getByText("")).toBeInTheDocument();
  });
});
